version: 4.0.78
steps:
  - prompt: Assert that the webpage loads.
    commands:
      - command: assert
        expect: TV Advertising That Performs is visible on the webpage
  - prompt: >-
      Assert that the logo is visible.  # Use the content of the file from the
      environment
    commands:
      - command: assert
        expect: tvScientific logo is visible on the webpage
